name: 'Action Test'
description: 'Action Test'
author: 'vmgabriel'
branding:
  icon: 'git-branch'
  color: 'gray-dark'
inputs:
  template:
    description: 'Template html file'
    required: true
    default: ''
  body:
    description: 'Body Content'
    required: true
    default: ''
  message_branch:
    description: 'Name of branch for generate module'
    required: true
    default: ''
outputs:
  html_data:
    description: 'Html Data'
  level:
    description: 'Level Content related to message commit if content message include {major|minor|patch}'
runs:
  using: 'docker'
  image: 'Dockerfile'
